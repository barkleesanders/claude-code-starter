---
name: scope-checker
description: "Merged into /carmack. Use /carmack instead."
---

# This skill has been merged into /carmack

All content from this skill is now built into `/carmack`.

**Use `/carmack` for all engineering work** — it includes:
- React variable scope detection before production deploys
- Catches: undefined child component references, missing optional chaining, stale closures
- Quick grep commands for scope issue detection
- Checklist: props, local state, imports, context only

```
/carmack [React scope issue or code to review]
```
