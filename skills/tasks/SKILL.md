---
name: tasks
description: "Merged into /carmack. Use /carmack instead."
---

# This skill has been merged into /carmack

All content from this skill is now built into `/carmack`.

**Use `/carmack` for all engineering work** — it includes:
- Convert PRD markdown to prd.json for autonomous execution loops
- Agent-testable acceptance criteria (boolean pass/fail only)
- 8-15 tasks per PRD, one concern per task, investigation before implementation
- Priority ordering: investigation → schema → backend → UI → verification
- Full example with 10 granular tasks from a single "fix signup page" PRD task

```
/carmack [convert PRD or create task list]
```
