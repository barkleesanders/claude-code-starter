---
name: agent-browser
description: "Merged into /carmack. Use /carmack instead."
---

# This skill has been merged into /carmack

All content from this skill is now built into `/carmack`.

**Use `/carmack` for all engineering work** — it includes:
- Full agent-browser CLI reference: open, snapshot, click, fill, type, scroll, wait
- Semantic locators, sessions, JSON output, video recording
- Debug mode (--headed), cookies/storage, network interception
- Example flows: form submission, auth with saved state, parallel sessions

```
/carmack [browser automation task]
```
