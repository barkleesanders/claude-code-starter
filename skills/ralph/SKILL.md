---
name: ralph
description: "Merged into /carmack. Use /carmack instead."
---

# This skill has been merged into /carmack

All content from this skill is now built into `/carmack`.

**Use `/carmack` for all engineering work** — it includes:
- Ralph Mode: autonomous feature implementation loop
- Creates `prd.json` with user stories, executes one at a time
- Story size rules (one context window), dependency ordering
- Progress tracking via `progress.txt` (append-only)
- Quality gates per language (TypeScript, Rust, Python)

```
/carmack [feature to implement autonomously]
```
